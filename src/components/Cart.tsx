import { Basket } from "@phosphor-icons/react";

const Cart = () => {
  return (
    <button>
      <Basket size={32} className="fill-on-hover transition-all" />
    </button>
  );
};

export { Cart };
